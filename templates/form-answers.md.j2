# Standard Form Answer Package - {{ job.title }}

These answers are generic copy-paste drafts. No application form was inspected for this package.

Application link:
{{ job.application_url or job.url }}

First name:
{{ contact.first_name | default(contact.name.split(" ")[0]) }}

Last name:
{{ contact.last_name | default(contact.name.split(" ")[-1]) }}

Email:
{{ contact.email }}

Phone:
{{ contact.phone }}

LinkedIn:
{{ contact.linkedin }}

Availability:
{{ availability.available_from | capitalize }}. {{ availability.logistics }}

Location / relocation:
Currently based in {{ contact.location }}. {{ location_policy.onsite_roles }}

Expected rate:
[Manual confirmation recommended]

Language requirements:
{{ job.required_languages | join(", ") if job.required_languages else "Manual confirmation recommended" }}

Message:
{{ application_text }}

Upload CV:
Use generated file: {{ cv_path }}
